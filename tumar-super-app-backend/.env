DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tumar_super_app_db
PORT=3000
# ВАЖНО: замените на случайный ключ длиной 64+ символа перед деплоем в продакшн
# Сгенерировать: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=tumar-super-app-jwt-secret-key-replace-before-production-deployment-2024
# AES-256 key (32 bytes = 64 hex chars) for encrypting virtual card CVV and expiry
# Regenerate before production: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CARD_ENCRYPTION_KEY=be10be012e3875ea5d4a54f592d5e30d5a8e92c7aa32e3df9ddbfb457a880a26

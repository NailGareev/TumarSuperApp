DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tumar_super_app_db
PORT=3000
# ВАЖНО: замените на случайный ключ длиной 64+ символа перед деплоем в продакшн
# Сгенерировать: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=tumar-super-app-jwt-secret-key-replace-before-production-deployment-2024
